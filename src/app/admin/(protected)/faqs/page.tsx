import { listAdminFaqs } from "@/data/repositories/faqs.repository";
import { requireAdmin } from "@/features/admin/auth/server";
import { FaqCard, NewFaqForm } from "@/features/admin/faqs/faq-forms";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic";
export default async function AdminFaqsPage() { await requireAdmin(); const faqs = await listAdminFaqs(await createClient()); return <><header className="mb-8"><p className="eyebrow">Conteúdo</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black text-olive sm:text-4xl">FAQ</h1><p className="mt-2 font-semibold text-stone-600">Gerencie perguntas, respostas, publicação e ordem.</p></div><a className="btn btn-primary" href="#nova-faq">Nova pergunta</a></div></header><NewFaqForm />{faqs.length ? <section aria-label="FAQs cadastradas" className="space-y-4">{faqs.map((faq) => <FaqCard key={faq.id} faq={faq} />)}</section> : <section className="rounded-2xl border border-dashed border-stone-300 bg-white p-8 text-center"><h2 className="text-xl font-black text-olive">Nenhuma FAQ cadastrada.</h2><a href="#nova-faq" className="btn btn-primary mt-4">Nova pergunta</a></section>}</>; }
